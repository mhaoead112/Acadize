// shared/schema.ts
// This file now serves as a re-export barrel from the authoritative schema
// Drizzle Kit and runtime both use server/src/db/schema.ts as the single source of truth

export * from '../server/src/db/schema.js';